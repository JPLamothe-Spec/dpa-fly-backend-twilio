# Voice (you already have verse; keep it for now)
VOICE=verse

# Faster turn-taking (trim waiting time after you stop talking)
VAD_SILENCE_MS=350
VAD_MAX_TURN_MS=1800
# ~100ms of μ-law @ 8 kHz to avoid empty commits
MIN_COMMIT_BYTES=800

# (optional) make intent super clear to the model
PROJECT_BRIEF="Digital Personal Assistant (DPA) Project. English-accented (UK/AU) female voice, warm and neutral. Keep replies concise; avoid asking for phone numbers unless requested."
